version: 0.2

phases:
  install:
    commands:
      - echo start [Development branch]
      - echo Insatlling NPM Packages and wget Enviorment File
      - npm install -g pnpm@6
  pre_build:
    commands:
      - pnpm install
  build:
    commands:
      - echo pnpm build start `date`
      - pnpm build:dev
  post_build:
    commands:
      - ls -al
      - echo Copy Mylab Build File
      - aws s3 cp --recursive dist/ s3://mylab.dev.amway.co.kr
      - echo Build completed on `date`
